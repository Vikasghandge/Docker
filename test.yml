---
   hosts: all
   become: yes
   task:
         - name: install nginx
           apt:
           name: nginx
           state: present

--- 
  - hosts: all(hostname)
    become: yes
    task: 
      - name: install nginx
        apt: 
        - name: nginx
          state: present           

--- 
  - hosts: all
    become: yes
    task: 
      - name: install nginx
         apt:
        - name: nginx        